input_size: 224
batch_size: 128
seed: 0
max_epochs: 100
patience: 10
replicates: 5
n_workers: 0
val_size: 0.1
learning_rate: 0.001
weight_decay: 0.01
data_dir: '../../data/mpic'
# data_dir: '../../../../../imgs'
ablation_classes: [aggregate, bubble, fiber_blur, fiber_sharp, long_pellet, mini_pellet, noise, short_pellet, swimmer]
all_classes: ['aggregate', 'bubble', 'fiber_blur', 'fiber_sharp', 'long_pellet', 'mini_pellet', 'noise', 'phyto_dino', 'phyto_long', 'phyto_round', 'rhizaria', 'salp_pellet', 'short_pellet', 'swimmer']
domains: [RR, SR, FK, JC]
domain_splits_fname: domain_splits.json
